// GENERATED by skal_codegen — do not edit by hand.
//
// Source: callbacks.dart
//
// One adapter per Widget class found in the source. Each adapter reads
// its constructor's named parameters from the bridge's custom-prop bag
// via NodeState.getCustomPropX(name, default), then constructs the
// real Flutter widget. The default value comes from the constructor's
// own default — if the JSX consumer omits a prop, they get the same
// behaviour as a direct Dart caller would.
//
// ignore_for_file: non_constant_identifier_names, sort_child_properties_last, unused_import

import 'package:flutter/material.dart';
import 'package:skal_flutter/skal/bridge.dart';
import 'package:skal_flutter/skal/node_state.dart';
import 'package:skal_flutter/skal/registry.dart';

import 'callbacks.dart';

Widget _build_Tappable(NodeState n, SkalBridge bridge) {
  return Tappable(
    label: n.getCustomPropStr('label') ?? 'Tap me',
    onTap: () => bridge.dispatchEvent(n.getCustomHandler('onTap')),
  );
}

Widget _build_Refreshable(NodeState n, SkalBridge bridge) {
  return Refreshable(
    label: n.getCustomPropStr('label') ?? 'pull',
    onRefresh: () => bridge.dispatchEvent(n.getCustomHandler('onRefresh')),
  );
}

Widget _build_Form(NodeState n, SkalBridge bridge) {
  return Form(
    onSwitch: (v) => bridge.dispatchEventBool(n.getCustomHandler('onSwitch'), v),
    onSlide: (v) => bridge.dispatchEventDouble(n.getCustomHandler('onSlide'), v),
    onPick: (v) => bridge.dispatchEventInt(n.getCustomHandler('onPick'), v),
  );
}

Widget _build_TapList(NodeState n, SkalBridge bridge) {
  return TapList(
    onItemTap: (index, payload) => bridge.dispatchEventTuple(n.getCustomHandler('onItemTap'), [index, payload]),
    onReorder: (oldIndex, newIndex) => bridge.dispatchEventTuple(n.getCustomHandler('onReorder'), [oldIndex, newIndex]),
  );
}

void registerAll() {
  SkalRegistry.registerWidget('tappable', _build_Tappable);
  SkalRegistry.registerWidget('refreshable', _build_Refreshable);
  SkalRegistry.registerWidget('form', _build_Form);
  SkalRegistry.registerWidget('tapList', _build_TapList);
}
