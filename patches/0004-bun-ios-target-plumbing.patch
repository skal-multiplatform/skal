diff --git a/build.zig b/build.zig
index 63b7297..b9d7ad0 100644
--- a/build.zig
+++ b/build.zig
@@ -55,6 +55,7 @@ const BunBuildOptions = struct {
     override_no_export_cpp_apis: bool,
     android_ndk_sysroot: ?[]const u8 = null,
     freebsd_sysroot: ?[]const u8 = null,
+    ios_sdkroot: ?[]const u8 = null,
 
     cached_options_module: ?*Module = null,
     windows_shim: ?WindowsShim = null,
@@ -113,6 +114,15 @@ pub fn getOSVersionMin(os: OperatingSystem) ?Target.Query.OsVersion {
             .semver = .{ .major = 13, .minor = 0, .patch = 0 },
         },
 
+        // iOS 16.0 — matches Skal's deployment target (see
+        // ios-app/iosApp/project.yml). iOS 15+ is required for libc++'s
+        // std::bit_cast (used in WebKit's bmalloc/Gigacage.h); bumping
+        // straight to 16 aligns with the rest of the Skal toolchain
+        // rather than picking the absolute floor.
+        .ios => .{
+            .semver = .{ .major = 16, .minor = 0, .patch = 0 },
+        },
+
         // FreeBSD 14.0 is the oldest supported major. 14.x guarantees forward
         // ABI compat, and 13.x is EOL by the time this lands.
         .freebsd => .{
@@ -175,6 +185,10 @@ pub fn build(b: *Build) !void {
             .linux => .linux,
             .freebsd => .freebsd,
             .windows => .windows,
+            // Skal: ios target (embedding bun as a JS runtime inside an
+            // iOS app — JIT must stay statically disabled; the bytecode
+            // cache path is the only viable runtime mode on device).
+            .ios => .ios,
             else => |t| std.debug.panic("Unsupported OS tag {}", .{t}),
         };
         const abi = temp_resolved.result.abi;
@@ -230,6 +244,21 @@ pub fn build(b: *Build) !void {
     if (os == .freebsd and freebsd_sysroot == null) {
         std.debug.panic("-Dfreebsd_sysroot is required when cross-compiling to FreeBSD (zig does not bundle FreeBSD libc headers)", .{});
     }
+    // Skal: iOS uses a separate SDK (iPhoneOS.sdk) for headers. Zig bundles
+    // macOS libc but NOT iOS libc — translate-c can't find <fcntl.h> etc.
+    // without an explicit -isysroot. On a darwin host we auto-detect via
+    // `xcrun --sdk iphoneos --show-sdk-path`; override via -Dios_sdkroot=...
+    const ios_sdkroot: ?[]const u8 = b.option([]const u8, "ios_sdkroot", "iOS SDK root for translate-c headers") orelse
+        if (os == .ios and builtin.os.tag == .macos) blk: {
+            const result = std.process.Child.run(.{
+                .allocator = b.allocator,
+                .argv = &.{ "xcrun", "--sdk", "iphoneos", "--show-sdk-path" },
+            }) catch break :blk null;
+            break :blk b.dupe(std.mem.trim(u8, result.stdout, "\n \t"));
+        } else null;
+    if (os == .ios and ios_sdkroot == null) {
+        std.debug.panic("-Dios_sdkroot is required when targeting iOS (run xcrun --sdk iphoneos --show-sdk-path)", .{});
+    }
 
     var build_options = BunBuildOptions{
         .target = target,
@@ -295,6 +324,7 @@ pub fn build(b: *Build) !void {
         .llvm_codegen_threads = b.option(u32, "llvm_codegen_threads", "Number of threads to use for LLVM codegen") orelse 1,
         .android_ndk_sysroot = android_ndk_sysroot,
         .freebsd_sysroot = freebsd_sysroot,
+        .ios_sdkroot = ios_sdkroot,
     };
 
     // zig build obj
@@ -320,7 +350,8 @@ pub fn build(b: *Build) !void {
                 .strip = false,
             }),
             .use_llvm = !build_options.no_llvm,
-            .use_lld = if (build_options.os == .mac) false else !build_options.no_llvm,
+            // Skal: ios is Mach-O like .mac (see addBunObject comment).
+            .use_lld = if (build_options.os == .mac or build_options.os == .ios) false else !build_options.no_llvm,
         });
         configureObj(b, &o, unit_tests);
         // Setting `linker_allow_shlib_undefined` causes the linker to ignore
@@ -511,7 +542,7 @@ pub fn build(b: *Build) !void {
         }) |t| {
             const resolved = t.resolveTarget(b);
             step.dependOn(
-                &b.addInstallFile(getTranslateC(b, resolved, .Debug, null, null), b.fmt("translated-c-headers/{s}.zig", .{
+                &b.addInstallFile(getTranslateC(b, resolved, .Debug, null, null, null), b.fmt("translated-c-headers/{s}.zig", .{
                     resolved.result.zigTriple(b.allocator) catch @panic("OOM"),
                 })).step,
             );
@@ -722,6 +753,7 @@ fn addMultiCheck(
                 .override_no_export_cpp_apis = root_build_options.override_no_export_cpp_apis,
                 .android_ndk_sysroot = root_build_options.android_ndk_sysroot,
                 .freebsd_sysroot = root_build_options.freebsd_sysroot,
+                .ios_sdkroot = root_build_options.ios_sdkroot,
             };
 
             var obj = addBunObject(b, &options);
@@ -731,7 +763,7 @@ fn addMultiCheck(
     }
 }
 
-fn getTranslateC(b: *Build, initial_target: std.Build.ResolvedTarget, optimize: std.builtin.OptimizeMode, android_ndk_sysroot: ?[]const u8, freebsd_sysroot: ?[]const u8) LazyPath {
+fn getTranslateC(b: *Build, initial_target: std.Build.ResolvedTarget, optimize: std.builtin.OptimizeMode, android_ndk_sysroot: ?[]const u8, freebsd_sysroot: ?[]const u8, ios_sdkroot: ?[]const u8) LazyPath {
     const target = b.resolveTargetQuery(q: {
         var query = initial_target.query;
         if (query.os_tag == .windows)
@@ -748,7 +780,12 @@ fn getTranslateC(b: *Build, initial_target: std.Build.ResolvedTarget, optimize:
         .{ "WINDOWS", translate_c.target.result.os.tag == .windows },
         .{ "POSIX", translate_c.target.result.os.tag != .windows },
         .{ "LINUX", translate_c.target.result.os.tag == .linux },
+        // DARWIN remains "any Apple OS" (macOS or iOS) for the common Mach-O /
+        // kqueue / pthread path. Use IOS to gate off the macOS-only headers
+        // and APIs that iOS sandboxes away (libproc, mount-table inspection,
+        // most of mach kernel surface, etc.).
         .{ "DARWIN", translate_c.target.result.os.tag.isDarwin() },
+        .{ "IOS", translate_c.target.result.os.tag == .ios },
         .{ "FREEBSD", translate_c.target.result.os.tag == .freebsd },
     }) |entry| {
         const str, const value = entry;
@@ -775,6 +812,14 @@ fn getTranslateC(b: *Build, initial_target: std.Build.ResolvedTarget, optimize:
         translate_c.addSystemIncludePath(.{ .cwd_relative = b.fmt("{s}/usr/include", .{sysroot}) });
     }
 
+    if (target.result.os.tag == .ios) {
+        // Skal: iOS SDK lives separate from Zig's bundled libc — translate-c
+        // needs /usr/include from iPhoneOS.sdk to resolve <fcntl.h> etc.
+        const sysroot = ios_sdkroot orelse
+            std.debug.panic("translate-c for iOS requires -Dios_sdkroot", .{});
+        translate_c.addSystemIncludePath(.{ .cwd_relative = b.fmt("{s}/usr/include", .{sysroot}) });
+    }
+
     if (target.result.os.tag == .windows) {
         // translate-c is unable to translate the unsuffixed windows functions
         // like `SetCurrentDirectory` since they are defined with an odd macro
@@ -845,7 +890,9 @@ fn configureObj(b: *Build, opts: *BunBuildOptions, obj: *Compile) void {
 
     // Object options
     obj.use_llvm = !opts.no_llvm;
-    obj.use_lld = if (opts.os == .mac or opts.os == .linux) false else !opts.no_llvm;
+    // Skal: ios is Mach-O like .mac — Zig's LLD support for Mach-O was
+    // dropped, so iOS targets must use the system linker too.
+    obj.use_lld = if (opts.os == .mac or opts.os == .linux or opts.os == .ios) false else !opts.no_llvm;
     if (opts.lto) {
         obj.lto = .full;
         obj.use_lld = true;
@@ -979,12 +1026,13 @@ fn addInternalImports(b: *Build, mod: *Module, opts: *BunBuildOptions) void {
 
     mod.addImport("build_options", opts.buildOptionsModule(b));
 
-    const translate_c = getTranslateC(b, opts.target, opts.optimize, opts.android_ndk_sysroot, opts.freebsd_sysroot);
+    const translate_c = getTranslateC(b, opts.target, opts.optimize, opts.android_ndk_sysroot, opts.freebsd_sysroot, opts.ios_sdkroot);
     mod.addImport("translated-c-headers", b.createModule(.{ .root_source_file = translate_c }));
 
     const zlib_internal_path = switch (os) {
         .windows => "src/zlib_sys/win32.zig",
-        .linux, .mac, .freebsd => "src/zlib_sys/posix.zig",
+        // Skal: ios shares the posix zlib internal (same libz interface).
+        .linux, .mac, .ios, .freebsd => "src/zlib_sys/posix.zig",
         else => null,
     };
     if (zlib_internal_path) |path| {
@@ -994,7 +1042,11 @@ fn addInternalImports(b: *Build, mod: *Module, opts: *BunBuildOptions) void {
     }
 
     const async_path = switch (os) {
-        .linux, .mac, .freebsd => "src/aio/posix_event_loop.zig",
+        // Skal: ios shares the posix (kqueue) path with mac/freebsd. iOS
+        // is a BSD-derived darwin platform; the same kqueue-based event
+        // loop works. Background-task restrictions are an iOS app-lifecycle
+        // concern, not an event-loop concern.
+        .linux, .mac, .freebsd, .ios => "src/aio/posix_event_loop.zig",
         .windows => "src/aio/windows_event_loop.zig",
         else => "src/aio/stub_event_loop.zig",
     };
diff --git a/packages/bun-usockets/src/eventing/epoll_kqueue.c b/packages/bun-usockets/src/eventing/epoll_kqueue.c
index 15e153c..86a5204 100644
--- a/packages/bun-usockets/src/eventing/epoll_kqueue.c
+++ b/packages/bun-usockets/src/eventing/epoll_kqueue.c
@@ -119,7 +119,14 @@ struct us_loop_t *us_timer_loop(struct us_timer_t *t) {
 #include <signal.h>
 #include <errno.h>
 
+// Android's seccomp filter for app processes blocks epoll_pwait2 (syscall 441)
+// with SIGSYS, which kills the process before the ENOSYS/EPERM fallback below
+// can run. Disable the syscall entirely on Android.
+#ifdef __ANDROID__
+static int has_epoll_pwait2 = 0;
+#else
 static int has_epoll_pwait2 = -1;
+#endif
 
 #ifndef SYS_epoll_pwait2
 // It's consistent on multiple architectures
diff --git a/scripts/build/config.ts b/scripts/build/config.ts
index fd527a2..577d666 100644
--- a/scripts/build/config.ts
+++ b/scripts/build/config.ts
@@ -17,7 +17,12 @@ import { clangTargetArch } from "./tools.ts";
 import { cyan, dim, green } from "./tty.ts";
 import { ZIG_COMMIT } from "./zig.ts";
 
-export type OS = "linux" | "darwin" | "windows" | "freebsd";
+// Skal: "ios" added as a separate target alongside "darwin" — they share the
+// Apple Mach-O / kqueue / libc family but differ on JIT availability,
+// sandboxed APIs (libproc, mach_vm), code signing requirements, and SDK
+// paths (iPhoneOS.sdk vs MacOSX.sdk). Code paths that should fire for both
+// use `cfg.apple` (computed below); macOS-only paths keep `cfg.darwin`.
+export type OS = "linux" | "darwin" | "windows" | "freebsd" | "ios";
 export type Arch = "x64" | "aarch64";
 export type Abi = "gnu" | "musl" | "android";
 export type BuildType = "Debug" | "Release" | "RelWithDebInfo" | "MinSizeRel";
@@ -70,12 +75,20 @@ export interface Config {
 
   // ─── Derived platform booleans (computed from os/arch) ───
   linux: boolean;
+  /** macOS specifically — `os === "darwin"`. NOT true for iOS. Use
+   * `apple` for the Apple OS family (macOS + iOS). */
   darwin: boolean;
   windows: boolean;
   freebsd: boolean;
-  /** linux || darwin || freebsd */
+  /** iOS device or Simulator target. Distinct from `darwin` so macOS-only
+   * checks don't accidentally fire for iOS. Skal-specific addition. */
+  ios: boolean;
+  /** darwin || ios — Apple OS family (Mach-O, libsystem.B, BSD libc,
+   * kqueue, codesign). Use for shared-Apple code paths. */
+  apple: boolean;
+  /** linux || darwin || ios || freebsd */
   unix: boolean;
-  /** darwin || freebsd — kqueue-based event loop */
+  /** darwin || ios || freebsd — kqueue-based event loop */
   kqueue: boolean;
   x64: boolean;
   arm64: boolean;
@@ -450,6 +463,12 @@ function ndkHostTag(host: Host): string {
       throw new BuildError("Android NDK does not ship FreeBSD prebuilts", {
         hint: "Cross-compile to Android from a Linux host",
       });
+    case "ios":
+      // iOS is a target-only OS in Skal — we never build FROM iOS, so this
+      // case is unreachable. Listed for switch exhaustiveness.
+      throw new BuildError("iOS cannot be the build host", {
+        hint: "iOS is a target-only OS; build from macOS",
+      });
   }
 }
 
@@ -538,8 +557,11 @@ export function resolveConfig(partial: PartialConfig, toolchain: Toolchain): Con
   const darwin = os === "darwin";
   const windows = os === "windows";
   const freebsd = os === "freebsd";
-  const unix = linux || darwin || freebsd;
-  const kqueue = darwin || freebsd;
+  // Skal: ios is a separate target alongside darwin; apple covers both.
+  const ios = os === "ios";
+  const apple = darwin || ios;
+  const unix = linux || apple || freebsd;
+  const kqueue = apple || freebsd;
   const x64 = arch === "x64";
   const arm64 = arch === "aarch64";
 
@@ -726,6 +748,29 @@ export function resolveConfig(partial: PartialConfig, toolchain: Toolchain): Con
     }
   }
 
+  // ─── Cross-compilation (iOS) ───
+  // Skal: iOS device targets cross-compile from a macOS host. clang's
+  // -target + -isysroot flags swap in iPhoneOS.sdk. JIT must be disabled
+  // at runtime via g_jscConfig.useJIT=false on startup (Apple's kernel
+  // blocks W^X for third-party apps; compile-time-on / runtime-off pattern
+  // matches the existing Android setup).
+  let iosMinVersion: string | undefined;
+  if (ios) {
+    if (host.os !== "darwin") {
+      throw new BuildError("--os=ios requires a macOS host (need Xcode + iPhoneOS SDK)", {
+        hint: "Build iOS targets from a Mac",
+      });
+    }
+    iosMinVersion = "16.0";
+    sysroot = execSync("xcrun --sdk iphoneos --show-sdk-path", { encoding: "utf8" }).trim();
+    if (!existsSync(sysroot)) {
+      throw new BuildError(`iPhoneOS SDK not found at ${sysroot}`, {
+        hint: "Install Xcode (xcrun --sdk iphoneos must work)",
+      });
+    }
+    crossTarget = `aarch64-apple-ios${iosMinVersion}`;
+  }
+
   // ─── Versioning ───
   const pkgJsonPath = resolve(cwd, "package.json");
   const pkgJson = JSON.parse(readFileSync(pkgJsonPath, "utf8")) as { version: string };
@@ -758,6 +803,8 @@ export function resolveConfig(partial: PartialConfig, toolchain: Toolchain): Con
     darwin,
     windows,
     freebsd,
+    ios,
+    apple,
     unix,
     kqueue,
     x64,
diff --git a/scripts/build/deps/cares.ts b/scripts/build/deps/cares.ts
index bc5539b..1d4a26c 100644
--- a/scripts/build/deps/cares.ts
+++ b/scripts/build/deps/cares.ts
@@ -77,7 +77,7 @@ export const cares: Dependency = {
         _POSIX_C_SOURCE: 200809,
         _XOPEN_SOURCE: 700,
       }),
-      ...(cfg.darwin && { _DARWIN_C_SOURCE: true }),
+      ...(cfg.apple && { _DARWIN_C_SOURCE: true }),
       ...(cfg.freebsd && { __BSD_VISIBLE: 1 }),
     },
     // _WIN32_WINNT must be the hex LITERAL 0x0602, not its decimal value —
@@ -279,7 +279,11 @@ function configH(cfg: Config): string {
   if (cfg.windows) {
     platform = WINDOWS;
     types = WINDOWS_SOCKET_TYPES;
-  } else if (cfg.darwin) {
+  } else if (cfg.apple) {
+    // Skal: ios shares macOS's kqueue + arc4random + sys/event/sockio
+    // surface. Without this, iOS falls through to the Linux branch and
+    // tries to include <malloc.h> + <sys/epoll.h> which don't exist on
+    // any Apple OS.
     platform = `${POSIX}\n${DARWIN}`;
     types = POSIX_SOCKET_TYPES;
   } else if (cfg.freebsd) {
diff --git a/scripts/build/deps/libarchive.ts b/scripts/build/deps/libarchive.ts
index b442210..edb42e2 100644
--- a/scripts/build/deps/libarchive.ts
+++ b/scripts/build/deps/libarchive.ts
@@ -284,7 +284,11 @@ const WINDOWS = def1([
 function configH(cfg: Config): string {
   const longSize = cfg.windows ? 4 : 8;
   const wcharSize = cfg.windows ? 2 : 4;
-  const platform = cfg.windows ? WINDOWS : `${POSIX}\n${cfg.darwin ? DARWIN : cfg.freebsd ? FREEBSD : LINUX}`;
+  // Skal: cfg.apple covers both macOS + iOS — they share the same darwin
+  // stat field set (st_atimespec, st_birthtimespec, etc.) and lack the
+  // Linux-only sys/sysmacros.h + linux/fs.h that the LINUX branch defines
+  // includes for.
+  const platform = cfg.windows ? WINDOWS : `${POSIX}\n${cfg.apple ? DARWIN : cfg.freebsd ? FREEBSD : LINUX}`;
 
   // Feature-test macros must come before any system header. archive_platform.h
   // includes config.h first, so defining them here is early enough.
diff --git a/scripts/build/deps/lolhtml.ts b/scripts/build/deps/lolhtml.ts
index cba437d..c3932c9 100644
--- a/scripts/build/deps/lolhtml.ts
+++ b/scripts/build/deps/lolhtml.ts
@@ -19,6 +19,10 @@ const LOLHTML_COMMIT = "77127cd2b8545998756e8d64e36ee2313c4bb312";
 function rustTargetTriple(cfg: Config): string {
   const arch = cfg.arm64 ? "aarch64" : "x86_64";
   if (cfg.darwin) return `${arch}-apple-darwin`;
+  // Skal: rustc's iOS device triple. Simulator would be aarch64-apple-ios-sim,
+  // but Skal's iOS-sim path uses the macOS prebuilt via vtool re-stamp;
+  // this build path is for real device.
+  if (cfg.ios) return `${arch}-apple-ios`;
   if (cfg.freebsd) return `${arch}-unknown-freebsd`;
   if (cfg.abi === "musl") return `${arch}-unknown-linux-musl`;
   return `${arch}-unknown-linux-gnu`;
@@ -79,8 +83,11 @@ export const lolhtml: Dependency = {
     // known to have both and -Zbuild-std for the target is verified
     // (linux-gnu, darwin, freebsd). musl/android keep the prebuilt-std
     // -Cpanic=abort path.
+    // Skal: iOS is Tier 2 in rustc — std is precompiled WITHOUT panic=abort,
+    // which doesn't satisfy our `-Cpanic=abort` rustflag. Add iOS to the
+    // build-std set so the toolchain rebuilds core/std with our panic strategy.
     const canBuildStdImmediateAbort =
-      cfg.darwin || cfg.freebsd || (cfg.linux && cfg.abi !== "musl" && cfg.abi !== "android");
+      cfg.apple || cfg.freebsd || (cfg.linux && cfg.abi !== "musl" && cfg.abi !== "android");
     if (cfg.release && canBuildStdImmediateAbort) {
       spec.buildStd = true;
       spec.rustflags = [
diff --git a/scripts/build/deps/webkit.ts b/scripts/build/deps/webkit.ts
index 3a03ee0..d4401e7 100644
--- a/scripts/build/deps/webkit.ts
+++ b/scripts/build/deps/webkit.ts
@@ -294,6 +294,27 @@ export const webkit: Dependency = {
             CMAKE_FIND_ROOT_PATH_MODE_INCLUDE: "BOTH",
           }
         : {}),
+      // Skal: iOS cross-compile from a macOS host. CMAKE_SYSTEM_NAME=Darwin
+      // (Apple's cmake module family treats iOS as a darwin variant);
+      // CMAKE_OSX_SYSROOT swaps to iPhoneOS SDK; arm64 only. Source of
+      // truth for these flags is scripts/build-jsc-ios.sh in the Skal
+      // repo — keep in sync.
+      ...(cfg.ios
+        ? {
+            CMAKE_SYSTEM_NAME: "Darwin",
+            CMAKE_SYSTEM_PROCESSOR: "arm64",
+            CMAKE_CROSSCOMPILING: "YES",
+            CMAKE_OSX_SYSROOT: cfg.sysroot!,
+            CMAKE_OSX_ARCHITECTURES: "arm64",
+            CMAKE_OSX_DEPLOYMENT_TARGET: "16.0",
+            USE_SYSTEM_MALLOC: "ON",
+            CMAKE_FIND_ROOT_PATH: cfg.sysroot!,
+            CMAKE_FIND_ROOT_PATH_MODE_PROGRAM: "NEVER",
+            CMAKE_FIND_ROOT_PATH_MODE_LIBRARY: "ONLY",
+            CMAKE_FIND_ROOT_PATH_MODE_INCLUDE: "ONLY",
+            CMAKE_FIND_ROOT_PATH_MODE_PACKAGE: "ONLY",
+          }
+        : {}),
       PORT: "JSCOnly",
       ENABLE_STATIC_JSC: "ON",
       USE_THIN_ARCHIVES: "OFF",
diff --git a/scripts/build/flags.ts b/scripts/build/flags.ts
index df55580..74229dc 100644
--- a/scripts/build/flags.ts
+++ b/scripts/build/flags.ts
@@ -469,9 +469,9 @@ export const bunOnlyFlags: Flag[] = [
   },
   {
     flag: "-std=c++23",
-    when: c => c.darwin,
+    when: c => c.apple,
     lang: "cxx",
-    desc: "C++23 standard",
+    desc: "C++23 standard (macOS + iOS — both Apple libc++)",
   },
   {
     flag: "/std:c++23preview",
diff --git a/scripts/build/profiles.ts b/scripts/build/profiles.ts
index c1e724e..ceb7acb 100644
--- a/scripts/build/profiles.ts
+++ b/scripts/build/profiles.ts
@@ -67,6 +67,21 @@ export const profiles = {
     webkit: "prebuilt",
   },
 
+  /**
+   * Skal: iOS device aarch64 cross-compile from a macOS host. Requires Xcode
+   * + iPhoneOS SDK (auto-detected via xcrun). JIT enabled at compile (matches
+   * bun's Android pattern); the iOS host app sets g_jscConfig.useJIT=false
+   * on startup, since Apple's kernel-level W^X blocks JIT in third-party
+   * apps. Sanitizers forced off (same reason as Android).
+   */
+  "ios-release": {
+    buildType: "Release",
+    os: "ios",
+    arch: "aarch64",
+    webkit: "local",
+    lto: false,
+  },
+
   /**
    * FreeBSD x64 cross-compile. Requires FREEBSD_SYSROOT (extracted base.txz).
    * Sanitizers are forced off in resolveConfig() regardless of profile.
diff --git a/scripts/build/tools.ts b/scripts/build/tools.ts
index 12f2f05..028586a 100644
--- a/scripts/build/tools.ts
+++ b/scripts/build/tools.ts
@@ -415,14 +415,20 @@ export function resolveLlvmToolchain(
   }
 
   // ld: ld.lld on Linux (passed as --ld-path=), lld-link on Windows.
-  // On Darwin clang drives the system linker directly.
+  // On Darwin clang drives the system linker directly for darwin targets,
+  // but cross-compiles (e.g. darwin → android) still pass --ld-path=, so
+  // we must find ld.lld on darwin too. findLlvmTool gracefully fails if
+  // not installed; the default install via `brew install lld@21`.
   let ld: string;
   if (os === "windows") {
     ld = findLlvmTool("lld-link", paths, os, { checkVersion: false, required: true })?.path ?? "";
   } else if (os === "linux") {
     ld = findLlvmTool("ld.lld", paths, os, { checkVersion: true, required: true })?.path ?? "";
   } else {
-    ld = ""; // darwin: unused
+    // darwin host: only required when cross-compiling to a target that
+    // needs lld. Best-effort lookup; the flags.ts gate is `when: c.linux`,
+    // so a darwin-only build keeps `c.ld = ""` and never emits the flag.
+    ld = findLlvmTool("ld.lld", paths, os, { checkVersion: false, required: false })?.path ?? "";
   }
 
   // strip: GNU strip on Linux (more features), llvm-strip elsewhere
diff --git a/scripts/build/zig.ts b/scripts/build/zig.ts
index 2e0129e..51ad6c0 100644
--- a/scripts/build/zig.ts
+++ b/scripts/build/zig.ts
@@ -91,6 +91,10 @@ export function zigObjectPaths(cfg: Config): string[] {
 export function zigTarget(cfg: Config): string {
   const arch = cfg.x64 ? "x86_64" : "aarch64";
   if (cfg.darwin) return `${arch}-macos-none`;
+  // Skal: iOS uses zig's "aarch64-ios.16" target triple. iOS-only ships
+  // on aarch64 (Apple Silicon devices + Simulator); no x64 iOS profile.
+  // iOS 16 floor — see build.zig getOSVersionMin comment.
+  if (cfg.ios) return `${arch}-ios.16.0`;
   if (cfg.windows) return `${arch}-windows-msvc`;
   if (cfg.freebsd) {
     assert(cfg.freebsdVersion !== undefined, "freebsd build missing version");
diff --git a/src/aio/posix_event_loop.zig b/src/aio/posix_event_loop.zig
index c37e6a7..a969315 100644
--- a/src/aio/posix_event_loop.zig
+++ b/src/aio/posix_event_loop.zig
@@ -535,7 +535,7 @@ pub const FilePoll = struct {
                 }
             } else if (kqueue_event.filter == std.c.EVFILT.PROC) {
                 flags.insert(Flags.process);
-            } else if (comptime Environment.isMac) {
+            } else if (comptime Environment.isApple) {
                 if (kqueue_event.filter == std.posix.system.EVFILT.MACHPORT) {
                     flags.insert(Flags.machport);
                 }
@@ -868,7 +868,7 @@ pub const FilePoll = struct {
                 this.deactivate(loop);
                 return errno;
             }
-        } else if (comptime Environment.isMac) {
+        } else if (comptime Environment.isApple) {
             var changelist = std.mem.zeroes([2]std.posix.system.kevent64_s);
             const one_shot_flag: u16 = if (!this.flags.contains(.one_shot))
                 0
@@ -1099,7 +1099,7 @@ pub const FilePoll = struct {
             if (bun.sys.Maybe(void).errnoSys(ctl, .epoll_ctl)) |errno| {
                 return errno;
             }
-        } else if (comptime Environment.isMac) {
+        } else if (comptime Environment.isApple) {
             var changelist = std.mem.zeroes([2]std.posix.system.kevent64_s);
 
             changelist[0] = switch (flag) {
@@ -1270,7 +1270,8 @@ pub const FilePoll = struct {
 };
 
 pub const Waker = switch (Environment.os) {
-    .mac => KEventWaker,
+    // Skal: ios shares the darwin kqueue-based waker.
+    .mac, .ios => KEventWaker,
     // FreeBSD 13+ has eventfd(2), so the Linux waker works as-is.
     .linux, .freebsd => LinuxWaker,
     .windows, .wasm => @compileError("unreachable"),
diff --git a/src/bun.zig b/src/bun.zig
index 5a3132e..5521b27 100644
--- a/src/bun.zig
+++ b/src/bun.zig
@@ -2004,7 +2004,7 @@ const WindowsStat = extern struct {
 
 pub const Stat = if (Environment.isWindows) windows.libuv.uv_stat_t else std.posix.Stat;
 pub const StatFS = switch (Environment.os) {
-    .mac, .linux, .freebsd => bun.c.struct_statfs,
+    .mac, .ios, .linux, .freebsd => bun.c.struct_statfs,
     .windows => windows.libuv.uv_statfs_t,
     .wasm => unreachable,
 };
diff --git a/src/bun_core/env.zig b/src/bun_core/env.zig
index 59f64e6..9460075 100644
--- a/src/bun_core/env.zig
+++ b/src/bun_core/env.zig
@@ -18,8 +18,13 @@ pub const isDebug = builtin.mode == .Debug;
 pub const isTest = builtin.is_test;
 pub const isLinux = builtin.target.os.tag == .linux;
 pub const isFreeBSD = builtin.target.os.tag == .freebsd;
-/// kqueue-based event loop (macOS + FreeBSD share most of this path).
-pub const isKqueue = isMac or isFreeBSD;
+// Skal: ios is darwin-family but distinct from macOS — many capabilities
+// (libproc, mount-table, mach kernel surface) are macOS-only. Most darwin
+// code paths still apply (kqueue, Mach-O, pthread).
+pub const isIos = build_target == .native and builtin.target.os.tag == .ios;
+pub const isApple = isMac or isIos;
+/// kqueue-based event loop (macOS + iOS + FreeBSD share most of this path).
+pub const isKqueue = isMac or isIos or isFreeBSD;
 pub const isAarch64 = builtin.target.cpu.arch.isAARCH64();
 pub const isX86 = builtin.target.cpu.arch.isX86();
 pub const isX64 = builtin.target.cpu.arch == .x86_64;
@@ -75,6 +80,9 @@ pub const OperatingSystem = enum {
     linux,
     freebsd,
     windows,
+    // Skal: ios target (build-time only — bun is embedded in a host iOS
+    // app as a dylib, the runtime doesn't sniff os.tag at this layer).
+    ios,
     // wAsM is nOt aN oPeRaTiNg SyStEm
     wasm,
 
@@ -105,6 +113,7 @@ pub const OperatingSystem = enum {
             .linux => "Linux",
             .freebsd => "FreeBSD",
             .windows => "Windows",
+            .ios => "iOS",
             .wasm => "WASM",
         };
     }
@@ -116,6 +125,10 @@ pub const OperatingSystem = enum {
             .linux => "linux",
             .freebsd => "freebsd",
             .windows => "win32",
+            // iOS is darwin at the libc + Mach-O level; userland JS would
+            // see process.platform === "darwin" too. The build target is
+            // distinct ("ios") but the runtime platform name stays darwin.
+            .ios => "darwin",
             .wasm => "wasm",
         };
     }
@@ -126,6 +139,7 @@ pub const OperatingSystem = enum {
             .linux => .linux,
             .freebsd => .freebsd,
             .windows => .windows,
+            .ios => .ios,
             .wasm => unreachable,
         };
     }
@@ -137,6 +151,7 @@ pub const OperatingSystem = enum {
             .linux => "linux",
             .freebsd => "freebsd",
             .windows => "windows",
+            .ios => "ios",
             .wasm => "wasm",
         };
     }
@@ -144,6 +159,8 @@ pub const OperatingSystem = enum {
 
 pub const os: OperatingSystem = if (isMac)
     .mac
+else if (isIos)
+    .ios
 else if (isLinux)
     .linux
 else if (isFreeBSD)
diff --git a/src/c-headers-for-zig.h b/src/c-headers-for-zig.h
index d08b86b..a0d18fb 100644
--- a/src/c-headers-for-zig.h
+++ b/src/c-headers-for-zig.h
@@ -39,12 +39,21 @@
 
 #if DARWIN
 #include <copyfile.h>
+#if !IOS
+// libproc, mach process inspection: macOS-only headers — iOS sandboxes
+// the underlying APIs away. Code that calls into them needs an
+// `Environment.os != .ios` guard on the Zig side (or a stub). Per
+// Skal's iOS port comment in build.zig.
 #include <libproc.h>
 #include <mach/mach_host.h>
 #include <mach/processor_info.h>
+#endif
+// sys/mount.h is present on iOS (struct_statfs is queryable inside the
+// app sandbox; mount/unmount ops fail at runtime with EPERM). Keep the
+// header.
+#include <sys/mount.h>
 #include <net/if_dl.h>
 #include <sys/clonefile.h>
-#include <sys/mount.h>
 #include <sys/stdio.h>
 #include <sys/sysctl.h>
 #elif LINUX
diff --git a/src/cli/upgrade_command.zig b/src/cli/upgrade_command.zig
index 1bc575b..e8034fe 100644
--- a/src/cli/upgrade_command.zig
+++ b/src/cli/upgrade_command.zig
@@ -36,7 +36,8 @@ pub const Version = struct {
     }
 
     pub const platform_label = switch (Environment.os) {
-        .mac => "darwin",
+        // Skal: ios reports as "darwin" for process.platform parity.
+        .mac, .ios => "darwin",
         .linux => "linux",
         .windows => "windows",
         .freebsd => "freebsd",
diff --git a/src/crash_handler/crash_handler.zig b/src/crash_handler/crash_handler.zig
index f641d38..3b80fbc 100644
--- a/src/crash_handler/crash_handler.zig
+++ b/src/crash_handler/crash_handler.zig
@@ -321,7 +321,7 @@ pub fn crashHandler(
                                 writer.print("(thread {d})", .{bun.c.GetCurrentThreadId()}) catch std.posix.abort();
                             }
                         },
-                        .mac, .linux, .freebsd => {},
+                        .mac, .ios, .linux, .freebsd => {},
                         .wasm => @compileError("TODO"),
                     }
 
@@ -866,7 +866,7 @@ const metadata_version_line = std.fmt.comptimePrint(
 fn handleSegfaultPosix(sig: i32, info: *const std.posix.siginfo_t, _: ?*const anyopaque) callconv(.c) noreturn {
     const addr = switch (bun.Environment.os) {
         .linux => @intFromPtr(info.fields.sigfault.addr),
-        .mac, .freebsd => @intFromPtr(info.addr),
+        .mac, .ios, .freebsd => @intFromPtr(info.addr),
         .windows, .wasm => @compileError("unreachable"),
     };
 
@@ -928,7 +928,7 @@ pub fn init() void {
         .windows => {
             windows_segfault_handle = windows.kernel32.AddVectoredExceptionHandler(0, handleSegfaultWindows);
         },
-        .mac, .linux, .freebsd => {
+        .mac, .ios, .linux, .freebsd => {
             resetOnPosix();
         },
         .wasm => @compileError("TODO"),
@@ -1150,6 +1150,10 @@ const Platform = enum(u8) {
     freebsd_x86_64_baseline = 'g',
     freebsd_aarch64 = 'F',
 
+    // Skal: ios only ships on aarch64 (Apple Silicon devices + Simulator);
+    // no x86_64 iOS target.
+    ios_aarch64 = 'i',
+
     const current = @field(Platform, @tagName(bun.Environment.os) ++
         "_" ++ @tagName(builtin.target.cpu.arch) ++
         (if (bun.Environment.baseline) "_baseline" else ""));
@@ -1201,7 +1205,8 @@ const StackLine = struct {
                     } else null,
                 };
             },
-            .mac => {
+            // Skal: ios shares the darwin _dyld_image_* API for module lookup.
+            .mac, .ios => {
                 // This code is slightly modified from std.debug.DebugInfo.lookupModuleNameDyld
                 // https://github.com/ziglang/zig/blob/215de3ee67f75e2405c177b262cb5c1cd8c8e343/lib/std/debug.zig#L1783
                 const address = if (addr == 0) 0 else addr - 1;
@@ -1545,7 +1550,7 @@ fn report(url: []const u8) void {
             // we don't care what happens with the process
             _ = spawn_result;
         },
-        .mac, .linux, .freebsd => {
+        .mac, .ios, .linux, .freebsd => {
             var buf: bun.PathBuffer = undefined;
             var buf2: bun.PathBuffer = undefined;
             const curl = bun.which(
diff --git a/src/install/npm.zig b/src/install/npm.zig
index c65a70d..0e21ef6 100644
--- a/src/install/npm.zig
+++ b/src/install/npm.zig
@@ -644,7 +644,11 @@ pub const OperatingSystem = enum(u16) {
 
     pub const current: OperatingSystem = switch (Environment.os) {
         .linux => @enumFromInt(if (Environment.isAndroid) android else linux),
-        .mac => @enumFromInt(darwin),
+        // Skal: ios maps to "darwin" for npm package.json "os" field matching
+        // (package authors don't have a dedicated ios bucket; iOS apps share
+        // darwin packages where it makes sense, even though most npm packages
+        // won't run on an embedded bun anyway).
+        .mac, .ios => @enumFromInt(darwin),
         .windows => @enumFromInt(win32),
         .freebsd => @enumFromInt(freebsd),
         .wasm => @compileError("Unsupported operating system: " ++ @tagName(Environment.os)),
diff --git a/src/main.zig b/src/main.zig
index 6698634..675002c 100644
--- a/src/main.zig
+++ b/src/main.zig
@@ -13,6 +13,17 @@ comptime {
     _bun.assert(builtin.target.cpu.arch.endian() == .little);
 }
 
+// Skal: pull in the JNI bridge so its @export'd symbols land in the bun-zig
+// build outputs. On non-embedded targets bun's main() drives the program
+// normally — the JNI symbols are unreferenced and would be GC'd by the
+// linker anyway. We include the import on darwin too so the host-side
+// libskal.dylib (used by desktop-app/) gets the same JNI surface.
+comptime {
+    if (builtin.target.abi.isAndroid() or builtin.target.os.tag == .macos) {
+        _ = @import("./skal_entry.zig");
+    }
+}
+
 extern fn bun_warn_avx_missing(url: [*:0]const u8) void;
 
 pub extern "c" var _environ: ?*anyopaque;
diff --git a/src/options_types/CompileTarget.zig b/src/options_types/CompileTarget.zig
index 1a942e3..fb6bb9e 100644
--- a/src/options_types/CompileTarget.zig
+++ b/src/options_types/CompileTarget.zig
@@ -298,6 +298,9 @@ pub fn isSupported(this: *const CompileTarget) bool {
         .windows => this.arch == .x64 or this.arch == .arm64,
 
         .mac => true,
+        // Skal: ios isn't a `bun build --compile` target — Apple disallows
+        // running downloaded executables in third-party apps.
+        .ios => false,
         .linux => true,
         .freebsd => true,
 
diff --git a/src/paths/resolve_path.zig b/src/paths/resolve_path.zig
index e7964a5..da7bdf5 100644
--- a/src/paths/resolve_path.zig
+++ b/src/paths/resolve_path.zig
@@ -959,7 +959,7 @@ pub const Platform = enum {
 
     pub const auto: Platform = switch (bun.Environment.os) {
         .windows => .windows,
-        .linux, .mac, .freebsd => .posix,
+        .linux, .mac, .ios, .freebsd => .posix,
         .wasm => .loose,
     };
 
diff --git a/src/runtime/api/bun/spawn.zig b/src/runtime/api/bun/spawn.zig
index 3fc7732..9c595a7 100644
--- a/src/runtime/api/bun/spawn.zig
+++ b/src/runtime/api/bun/spawn.zig
@@ -341,7 +341,7 @@ pub const PosixSpawn = struct {
         //   setsid() + ioctl(TIOCSCTTY) before exec, which system posix_spawn can't do.
         //   For non-PTY spawns on macOS, we use system posix_spawn which is safer
         //   (Apple's posix_spawn uses a kernel fast-path that avoids fork() entirely).
-        const use_bun_spawn = Environment.isLinux or Environment.isFreeBSD or (Environment.isMac and pty_slave_fd >= 0);
+        const use_bun_spawn = Environment.isLinux or Environment.isFreeBSD or (Environment.isApple and pty_slave_fd >= 0);
 
         if (use_bun_spawn) {
             return BunSpawnRequest.spawn(
@@ -365,9 +365,11 @@ pub const PosixSpawn = struct {
             );
         }
 
-        // macOS without PTY: use system posix_spawn
-        // Need to convert BunSpawn.Actions to PosixSpawnActions for system posix_spawn
-        if (comptime Environment.isMac) {
+        // darwin (macOS + iOS) without PTY: use system posix_spawn.
+        // Need to convert BunSpawn.Actions to PosixSpawnActions for system
+        // posix_spawn. Note: iOS app sandboxes typically block posix_spawn
+        // entirely; this code path compiles but will likely fail at runtime.
+        if (comptime Environment.isApple) {
             var posix_actions = PosixSpawnActions.init() catch return Maybe(pid_t){
                 .err = .{
                     .errno = @intFromEnum(std.c.E.NOMEM),
diff --git a/src/runtime/ffi/ffi.zig b/src/runtime/ffi/ffi.zig
index 59baaf4..c335ea0 100644
--- a/src/runtime/ffi/ffi.zig
+++ b/src/runtime/ffi/ffi.zig
@@ -1033,7 +1033,7 @@ pub const FFI = struct {
                 name_slice.slice(),
                 switch (Environment.os) {
                     .linux, .freebsd => "so",
-                    .mac => "dylib",
+                    .mac, .ios => "dylib",
                     .windows => "dll",
                     .wasm => @compileError("TODO"),
                 },
diff --git a/src/runtime/node/StatFS.zig b/src/runtime/node/StatFS.zig
index 03d5f8e..4857a85 100644
--- a/src/runtime/node/StatFS.zig
+++ b/src/runtime/node/StatFS.zig
@@ -47,7 +47,7 @@ pub fn StatFSType(comptime big: bool) type {
 
         pub fn init(statfs_: *const bun.StatFS) This {
             const fstype_, const bsize_, const blocks_, const bfree_, const bavail_, const files_, const ffree_ = switch (comptime Environment.os) {
-                .linux, .mac, .freebsd => .{
+                .linux, .mac, .ios, .freebsd => .{
                     statfs_.f_type,
                     statfs_.f_bsize,
                     statfs_.f_blocks,
diff --git a/src/runtime/node/dir_iterator.zig b/src/runtime/node/dir_iterator.zig
index 938f0ad..7c7cca4 100644
--- a/src/runtime/node/dir_iterator.zig
+++ b/src/runtime/node/dir_iterator.zig
@@ -35,7 +35,8 @@ pub const IteratorW = NewIterator(true);
 
 pub fn NewIterator(comptime use_windows_ospath: bool) type {
     return switch (bun.Environment.os) {
-        .mac => struct {
+        // Skal: ios shares the darwin getdirentries(2) iterator.
+        .mac, .ios => struct {
             dir: FD,
             seek: i64,
             buf: [8192]u8 align(@alignOf(std.posix.system.dirent)),
@@ -501,7 +502,7 @@ pub fn NewWrappedIterator(comptime path_type: PathType) type {
         pub fn init(dir: FD) Self {
             return Self{
                 .iter = switch (bun.Environment.os) {
-                    .mac,
+                    .mac, .ios,
                     => IteratorType{
                         .dir = dir,
                         .seek = 0,
diff --git a/src/runtime/node/node_fs.zig b/src/runtime/node/node_fs.zig
index 98a2646..c2f5fdb 100644
--- a/src/runtime/node/node_fs.zig
+++ b/src/runtime/node/node_fs.zig
@@ -3561,7 +3561,10 @@ pub const NodeFS = struct {
         const ret = Maybe(Return.CopyFile);
 
         // TODO: do we need to fchown?
-        if (comptime Environment.isMac) {
+        // Skal: ios shares the same copyfile/clonefile APIs as macOS — both
+        // are part of libsystem and exposed on iOS (the APFS clonefile syscall
+        // works inside the app sandbox).
+        if (comptime Environment.isApple) {
             var src_buf: bun.PathBuffer = undefined;
             var dest_buf: bun.PathBuffer = undefined;
 
diff --git a/src/runtime/node/node_os.zig b/src/runtime/node/node_os.zig
index 2d927c4..f09e367 100644
--- a/src/runtime/node/node_os.zig
+++ b/src/runtime/node/node_os.zig
@@ -40,6 +40,12 @@ pub fn cpus(global: *jsc.JSGlobalObject) bun.JSError!jsc.JSValue {
     const cpusImpl = switch (Environment.os) {
         .linux => cpusImplLinux,
         .mac => cpusImplDarwin,
+        // Skal: ios can't use host_processor_info / mach_host APIs (restricted
+        // in third-party app sandbox). Real iOS apps rarely need os.cpus() —
+        // stub returns an empty array. NSProcessInfo.processorCount is the
+        // sanctioned API for CPU count on iOS, expose via a separate binding
+        // if a use case appears.
+        .ios => cpusImplIosStub,
         .freebsd => cpusImplFreeBSD,
         .windows => cpusImplWindows,
         .wasm => @compileError("Unsupported OS"),
@@ -54,6 +60,14 @@ pub fn cpus(global: *jsc.JSGlobalObject) bun.JSError!jsc.JSValue {
     };
 }
 
+// Skal: iOS stub for os.cpus(). Real iOS apps can't query host-wide CPU info
+// (mach_host APIs are restricted in 3rd-party app sandbox). Returns an empty
+// array; JS callers should treat absence as "info unavailable" rather than
+// "zero CPUs."
+fn cpusImplIosStub(globalThis: *jsc.JSGlobalObject) !jsc.JSValue {
+    return try jsc.JSValue.createEmptyArray(globalThis, 0);
+}
+
 fn cpusImplLinux(globalThis: *jsc.JSGlobalObject) !jsc.JSValue {
     // Create the return array
     const values = try jsc.JSValue.createEmptyArray(globalThis, 0);
@@ -476,7 +490,7 @@ pub fn hostname(global: *jsc.JSGlobalObject) bun.JSError!jsc.JSValue {
 
 pub fn loadavg(global: *jsc.JSGlobalObject) bun.JSError!jsc.JSValue {
     const result = switch (bun.Environment.os) {
-        .mac => loadavg: {
+        .mac, .ios => loadavg: {
             var avg: c.struct_loadavg = undefined;
             var size: usize = @sizeOf(@TypeOf(avg));
 
@@ -525,7 +539,7 @@ pub fn loadavg(global: *jsc.JSGlobalObject) bun.JSError!jsc.JSValue {
 }
 
 pub const networkInterfaces = switch (Environment.os) {
-    .linux, .mac, .freebsd => networkInterfacesPosix,
+    .linux, .mac, .ios, .freebsd => networkInterfacesPosix,
     .windows => networkInterfacesWindows,
     .wasm => @compileError("Unsupported OS"),
 };
@@ -571,7 +585,7 @@ fn networkInterfacesPosix(globalThis: *jsc.JSGlobalObject) bun.JSError!jsc.JSVal
             if (iface.ifa_addr == null) return false;
             return if (comptime Environment.isLinux)
                 return iface.ifa_addr.*.sa_family == std.posix.AF.PACKET
-            else if (comptime Environment.isMac or Environment.isFreeBSD)
+            else if (comptime Environment.isApple or Environment.isFreeBSD)
                 return iface.ifa_addr.?.*.sa_family == std.posix.AF.LINK
             else
                 @compileError("unreachable");
@@ -667,7 +681,7 @@ fn networkInterfacesPosix(globalThis: *jsc.JSGlobalObject) bun.JSError!jsc.JSVal
                 //  cast to a link-layer socket address
                 if (comptime Environment.isLinux) {
                     break @as(?*std.posix.sockaddr.ll, @ptrCast(@alignCast(ll_iface.ifa_addr)));
-                } else if (comptime Environment.isMac or Environment.isFreeBSD) {
+                } else if (comptime Environment.isApple or Environment.isFreeBSD) {
                     break @as(?*c.sockaddr_dl, @ptrCast(@alignCast(ll_iface.ifa_addr)));
                 } else {
                     @compileError("unreachable");
@@ -678,7 +692,7 @@ fn networkInterfacesPosix(globalThis: *jsc.JSGlobalObject) bun.JSError!jsc.JSVal
                 // Encode its link-layer address.  We need 2*6 bytes for the
                 //  hex characters and 5 for the colon separators
                 var mac_buf: [17]u8 = undefined;
-                const addr_data = if (comptime Environment.isLinux) ll_addr.addr else if (comptime Environment.isMac or Environment.isFreeBSD) ll_addr.sdl_data[ll_addr.sdl_nlen..] else @compileError("unreachable");
+                const addr_data = if (comptime Environment.isLinux) ll_addr.addr else if (comptime Environment.isApple or Environment.isFreeBSD) ll_addr.sdl_data[ll_addr.sdl_nlen..] else @compileError("unreachable");
                 if (addr_data.len < 6) {
                     const mac = "00:00:00:00:00:00";
                     interface.put(globalThis, jsc.ZigString.static("mac"), jsc.ZigString.init(mac).withEncoding().toJS(globalThis));
@@ -845,7 +859,7 @@ pub fn release() bun.String {
 
             break :slice name_buffer[0..result.len];
         },
-        .mac, .freebsd => slice: {
+        .mac, .ios, .freebsd => slice: {
             @memset(&name_buffer, 0);
 
             var size: usize = name_buffer.len;
@@ -940,7 +954,7 @@ pub fn setPriority2(global: *jsc.JSGlobalObject, priority: i32) !void {
 
 pub fn totalmem() u64 {
     switch (bun.Environment.os) {
-        .mac => {
+        .mac, .ios => {
             var memory_: [32]c_ulonglong = undefined;
             var size: usize = memory_.len;
 
@@ -990,7 +1004,7 @@ pub fn uptime(global: *jsc.JSGlobalObject) bun.JSError!f64 {
             }
             return uptime_value;
         },
-        .mac, .freebsd => {
+        .mac, .ios, .freebsd => {
             var boot_time: std.posix.timeval = undefined;
             var size: usize = @sizeOf(@TypeOf(boot_time));
 
@@ -1047,7 +1061,7 @@ pub fn version() bun.JSError!bun.String {
     var name_buffer: [bun.HOST_NAME_MAX]u8 = undefined;
 
     const slice: []const u8 = switch (Environment.os) {
-        .mac, .freebsd => slice: {
+        .mac, .ios, .freebsd => slice: {
             @memset(&name_buffer, 0);
 
             var size: usize = name_buffer.len;
diff --git a/src/runtime/node/path_watcher.zig b/src/runtime/node/path_watcher.zig
index 4e58483..ffcd66b 100644
--- a/src/runtime/node/path_watcher.zig
+++ b/src/runtime/node/path_watcher.zig
@@ -396,7 +396,9 @@ fn walkSubtree(
 
 const Platform = switch (Environment.os) {
     .linux => Linux,
-    .mac => Darwin,
+    // Skal: ios shares the darwin FSEvents backend (same Core Services API
+    // available on iOS as on macOS for path watching within app sandbox).
+    .mac, .ios => Darwin,
     .freebsd => Kqueue,
     // win_watcher.zig imports PathWatcher.EventType from this file, so this type must
     // resolve on Windows even though none of the code paths run. The stub keeps the
@@ -940,7 +942,11 @@ const Kqueue = struct {
     }
 };
 
-const FSEvents = if (Environment.isMac) @import("./fs_events.zig") else struct {};
+// Skal: iOS technically has FSEvents (Core Services framework) but the daemon
+// running it (fseventsd) isn't accessible to sandboxed third-party apps. For
+// now share the macOS import — code paths that try to use FSEvents at runtime
+// on iOS will fail gracefully when the FSEventStream callbacks never fire.
+const FSEvents = if (Environment.isApple) @import("./fs_events.zig") else struct {};
 
 const std = @import("std");
 
diff --git a/src/runtime/webcore/blob/copy_file.zig b/src/runtime/webcore/blob/copy_file.zig
index 87e06b2..9b9ef09 100644
--- a/src/runtime/webcore/blob/copy_file.zig
+++ b/src/runtime/webcore/blob/copy_file.zig
@@ -431,7 +431,7 @@ pub const CopyFile = struct {
 
             // First, we attempt to clonefile() on macOS
             // This is the fastest way to copy a file.
-            if (comptime Environment.isMac) {
+            if (comptime Environment.isApple) {
                 if (this.offset == 0 and this.source_file_store.pathlike == .path and this.destination_file_store.pathlike == .path) {
                     do_clonefile: {
                         var path_buf: bun.PathBuffer = undefined;
@@ -586,7 +586,7 @@ pub const CopyFile = struct {
             return;
         }
 
-        if (comptime Environment.isMac) {
+        if (comptime Environment.isApple) {
             this.doFCopyFileWithReadWriteLoopFallback() catch {
                 this.doClose();
 
diff --git a/src/shell/interpreter.zig b/src/shell/interpreter.zig
index 1e34c59..878be73 100644
--- a/src/shell/interpreter.zig
+++ b/src/shell/interpreter.zig
@@ -2117,9 +2117,9 @@ pub fn isPollable(fd: bun.FD, mode: bun.Mode) bool {
     return switch (bun.Environment.os) {
         .windows, .wasm => false,
         .linux, .freebsd => posix.S.ISFIFO(mode) or posix.S.ISSOCK(mode) or posix.isatty(fd.native()),
-        // macos DOES allow regular files to be pollable, but we don't want that because
-        // our IOWriter code has a separate and better codepath for writing to files.
-        .mac => if (posix.S.ISREG(mode)) false else posix.S.ISFIFO(mode) or posix.S.ISSOCK(mode) or posix.isatty(fd.native()),
+        // darwin (macOS + iOS) DOES allow regular files to be pollable, but we
+        // don't want that — IOWriter has a separate, faster codepath for files.
+        .mac, .ios => if (posix.S.ISREG(mode)) false else posix.S.ISFIFO(mode) or posix.S.ISSOCK(mode) or posix.isatty(fd.native()),
     };
 }
 
@@ -2127,9 +2127,7 @@ pub fn isPollableFromMode(mode: bun.Mode) bool {
     return switch (bun.Environment.os) {
         .windows, .wasm => false,
         .linux, .freebsd => posix.S.ISFIFO(mode) or posix.S.ISSOCK(mode),
-        // macos DOES allow regular files to be pollable, but we don't want that because
-        // our IOWriter code has a separate and better codepath for writing to files.
-        .mac => if (posix.S.ISREG(mode)) false else posix.S.ISFIFO(mode) or posix.S.ISSOCK(mode),
+        .mac, .ios => if (posix.S.ISREG(mode)) false else posix.S.ISFIFO(mode) or posix.S.ISSOCK(mode),
     };
 }
 
diff --git a/src/standalone_graph/StandaloneModuleGraph.zig b/src/standalone_graph/StandaloneModuleGraph.zig
index c9d5ab8..859ecfb 100644
--- a/src/standalone_graph/StandaloneModuleGraph.zig
+++ b/src/standalone_graph/StandaloneModuleGraph.zig
@@ -1268,7 +1268,11 @@ pub const StandaloneModuleGraph = struct {
     /// Loads the standalone module graph from the executable, allocates it on the heap,
     /// sets it globally, and returns the pointer.
     pub fn fromExecutable(allocator: std.mem.Allocator) !?*StandaloneModuleGraph {
-        if (comptime Environment.isMac) {
+        // Skal: ios uses Mach-O like macOS — same trailer-reading path.
+        // (In practice, embedded bun on iOS isn't a standalone compiled
+        // binary, so this path is dead code at runtime; we just need it
+        // to compile.)
+        if (comptime Environment.isApple) {
             const macho_bytes = Macho.getData() orelse return null;
             if (macho_bytes.len < @sizeOf(Offsets) + trailer.len) {
                 Output.debugWarn("bun standalone module graph is too small to be valid", .{});
diff --git a/src/sys/coreutils_error_map.zig b/src/sys/coreutils_error_map.zig
index 2a0f28a..0d0ec95 100644
--- a/src/sys/coreutils_error_map.zig
+++ b/src/sys/coreutils_error_map.zig
@@ -139,7 +139,7 @@ pub const coreutils_error_map = brk: {
         },
         // Mac has slightly different messages. To keep it consistent with bash/coreutils,
         // it will use those altered messages.
-        .mac => &.{
+        .mac, .ios => &.{
             .{ "E2BIG", "Argument list too long" },
             .{ "EACCES", "Permission denied" },
             .{ "EADDRINUSE", "Address already in use" },
diff --git a/src/sys/fd.zig b/src/sys/fd.zig
index 20a0343..46e95fa 100644
--- a/src/sys/fd.zig
+++ b/src/sys/fd.zig
@@ -268,7 +268,8 @@ pub const FD = packed struct(backing_int) {
                     else => null,
                 };
             },
-            .mac => result: {
+            // Skal: ios shares darwin's libsystem close$NOCANCEL.
+            .mac, .ios => result: {
                 bun.assert(fd.native() >= 0);
                 break :result switch (bun.sys.getErrno(bun.sys.syscall.@"close$NOCANCEL"(fd.native()))) {
                     .BADF => .{ .errno = @intFromEnum(E.BADF), .syscall = .close, .fd = fd },
diff --git a/src/sys/sys.zig b/src/sys/sys.zig
index 3de403f..713182f 100644
--- a/src/sys/sys.zig
+++ b/src/sys/sys.zig
@@ -13,7 +13,8 @@ const This = @This();
 const platform_defs = switch (Environment.os) {
     .windows => @import("../errno/windows_errno.zig"),
     .linux => @import("../errno/linux_errno.zig"),
-    .mac => @import("../errno/darwin_errno.zig"),
+    // Skal: ios shares the darwin errno table (same kernel ABI family).
+    .mac, .ios => @import("../errno/darwin_errno.zig"),
     .freebsd => @import("../errno/freebsd_errno.zig"),
     .wasm => {},
 };
@@ -52,8 +53,8 @@ pub const syslog = log;
 
 pub const syscall = switch (Environment.os) {
     .linux => std.os.linux,
-    // macOS and FreeBSD go through libc
-    .mac, .freebsd => std.c,
+    // macOS, iOS, and FreeBSD go through libc.
+    .mac, .ios, .freebsd => std.c,
     .windows, .wasm => @compileError("not implemented"),
 };
 
@@ -64,7 +65,8 @@ fn toPackedO(number: anytype) std.posix.O {
 pub const Mode = std.posix.mode_t;
 
 pub const O = switch (Environment.os) {
-    .mac => struct {
+    // Skal: ios shares the darwin O flags (BSD-derived).
+    .mac, .ios => struct {
         pub const PATH = 0x0000;
         pub const RDONLY = 0x0000;
         pub const WRONLY = 0x0001;
@@ -552,7 +554,7 @@ pub fn statfs(path: [:0]const u8) Maybe(bun.StatFS) {
             var statfs_ = mem.zeroes(bun.StatFS);
             const rc = if (Environment.isLinux)
                 c.statfs(path, &statfs_)
-            else if (Environment.isMac)
+            else if (Environment.isApple)
                 c.statfs(path, &statfs_)
             else if (Environment.isFreeBSD)
                 c.statfs(path, &statfs_)
@@ -806,7 +808,7 @@ pub fn mkdiratA(dir_fd: bun.FD, file_path: []const u8) Maybe(void) {
 
 pub fn mkdiratZ(dir_fd: bun.FD, file_path: [*:0]const u8, mode: mode_t) Maybe(void) {
     return switch (Environment.os) {
-        .mac => Maybe(void).errnoSysP(syscall.mkdirat(@intCast(dir_fd.cast()), file_path, mode), .mkdir, file_path) orelse .success,
+        .mac, .ios => Maybe(void).errnoSysP(syscall.mkdirat(@intCast(dir_fd.cast()), file_path, mode), .mkdir, file_path) orelse .success,
         .linux => Maybe(void).errnoSysP(linux.mkdirat(@intCast(dir_fd.cast()), file_path, mode), .mkdir, file_path) orelse .success,
         .freebsd => Maybe(void).errnoSysP(syscall.mkdirat(@intCast(dir_fd.cast()), file_path, mode), .mkdir, file_path) orelse .success,
         .windows, .wasm => @compileError("mkdir is not implemented on this platform"),
@@ -886,7 +888,7 @@ pub fn lstatat(fd: bun.FD, path: [:0]const u8) Maybe(bun.Stat) {
 
 pub fn mkdir(file_path: [:0]const u8, flags: mode_t) Maybe(void) {
     return switch (Environment.os) {
-        .mac, .freebsd => Maybe(void).errnoSysP(syscall.mkdir(file_path, flags), .mkdir, file_path) orelse .success,
+        .mac, .ios, .freebsd => Maybe(void).errnoSysP(syscall.mkdir(file_path, flags), .mkdir, file_path) orelse .success,
 
         .linux => Maybe(void).errnoSysP(syscall.mkdir(file_path, flags), .mkdir, file_path) orelse .success,
 
@@ -1849,7 +1851,8 @@ pub fn write(fd: bun.FD, bytes: []const u8) Maybe(usize) {
     }
 
     return switch (Environment.os) {
-        .mac => {
+        // Skal: ios shares the darwin libsystem nocancel write.
+        .mac, .ios => {
             const rc = darwin_nocancel.@"write$NOCANCEL"(fd.cast(), bytes.ptr, adjusted_len);
             log("write({f}, {d}) = {d} ({f})", .{ fd, adjusted_len, rc, debug_timer });
 
@@ -2135,7 +2138,8 @@ pub fn read(fd: bun.FD, buf: []u8) Maybe(usize) {
     const debug_timer = bun.Output.DebugTimer.start();
     const adjusted_len = @min(buf.len, max_count);
     return switch (Environment.os) {
-        .mac => {
+        // Skal: ios shares the darwin libsystem nocancel read.
+        .mac, .ios => {
             const rc = darwin_nocancel.@"read$NOCANCEL"(fd.cast(), buf.ptr, adjusted_len);
 
             if (Maybe(usize).errnoSysFd(rc, .read, fd)) |err| {
@@ -2620,7 +2624,7 @@ pub fn renameat2(from_dir: bun.FD, from: [:0]const u8, to_dir: bun.FD, to: [:0]c
     while (true) {
         const rc = switch (comptime Environment.os) {
             .linux => std.os.linux.renameat2(@intCast(from_dir.cast()), from.ptr, @intCast(to_dir.cast()), to.ptr, flags.int()),
-            .mac => bun.c.renameatx_np(@intCast(from_dir.cast()), from.ptr, @intCast(to_dir.cast()), to.ptr, flags.int()),
+            .mac, .ios => bun.c.renameatx_np(@intCast(from_dir.cast()), from.ptr, @intCast(to_dir.cast()), to.ptr, flags.int()),
             .freebsd => unreachable, // returned above
             .windows, .wasm => @compileError("renameat2() is not implemented on this platform"),
         };
@@ -2842,7 +2846,7 @@ pub fn symlinkW(dest: [:0]const u16, target: [:0]const u16, options: WindowsSyml
 }
 
 pub fn clonefile(from: [:0]const u8, to: [:0]const u8) Maybe(void) {
-    if (comptime !Environment.isMac) @compileError("macOS only");
+    if (comptime !Environment.isApple) @compileError("darwin (macOS or iOS) only");
 
     while (true) {
         if (Maybe(void).errnoSys(c.clonefile(from, to, 0), .clonefile)) |err| {
@@ -2888,7 +2892,7 @@ pub fn clonefileat(from: FD, from_path: [:0]const u8, to: FD, to_path: [:0]const
 }
 
 pub fn copyfile(from: [:0]const u8, to: [:0]const u8, flags: posix.system.COPYFILE) Maybe(void) {
-    if (comptime !Environment.isMac) @compileError("macOS only");
+    if (comptime !Environment.isApple) @compileError("darwin (macOS or iOS) only");
 
     while (true) {
         if (Maybe(void).errnoSys(c.copyfile(from, to, null, flags), .copyfile)) |err| {
@@ -2900,7 +2904,7 @@ pub fn copyfile(from: [:0]const u8, to: [:0]const u8, flags: posix.system.COPYFI
 }
 
 pub fn fcopyfile(fd_in: bun.FD, fd_out: bun.FD, flags: posix.system.COPYFILE) Maybe(void) {
-    if (comptime !Environment.isMac) @compileError("macOS only");
+    if (comptime !Environment.isApple) @compileError("darwin (macOS or iOS) only");
 
     while (true) {
         if (Maybe(void).errnoSys(syscall.fcopyfile(fd_in.cast(), fd_out.cast(), null, flags), .fcopyfile)) |err| {
@@ -3016,9 +3020,11 @@ pub fn getFdPath(fd: bun.FD, out_buffer: *bun.PathBuffer) Maybe([]u8) {
             // Trust that Windows gives us valid UTF-16LE.
             return .{ .result = @constCast(bun.strings.fromWPath(out_buffer, wide_slice)) };
         },
-        .mac => {
-            // On macOS, we can use F.GETPATH fcntl command to query the OS for
-            // the path to the file descriptor.
+        // Skal: ios shares macOS's F.GETPATH fcntl semantics — same darwin
+        // VFS layer, same syscall path.
+        .mac, .ios => {
+            // On darwin (macOS + iOS), we can use F.GETPATH fcntl command to
+            // query the OS for the path to the file descriptor.
             @memset(out_buffer[0..out_buffer.*.len], 0);
             switch (fcntl(fd, posix.F.GETPATH, @intFromPtr(out_buffer))) {
                 .err => |err| return .{ .err = err },
@@ -3862,7 +3868,7 @@ pub fn setFileOffset(fd: bun.FD, offset: usize) Maybe(void) {
         ) orelse .success;
     }
 
-    if (comptime Environment.isMac or Environment.isFreeBSD) {
+    if (comptime Environment.isApple or Environment.isFreeBSD) {
         return Maybe(void).errnoSysFd(
             std.c.lseek(fd.cast(), @intCast(offset), posix.SEEK.SET),
             .lseek,
diff --git a/src/uws_sys/Loop.zig b/src/uws_sys/Loop.zig
index 58ad6a0..582f1e7 100644
--- a/src/uws_sys/Loop.zig
+++ b/src/uws_sys/Loop.zig
@@ -25,7 +25,8 @@ pub const PosixLoop = extern struct {
 
     const EventType = switch (Environment.os) {
         .linux => std.os.linux.epoll_event,
-        .mac => std.posix.system.kevent64_s,
+        // Skal: ios shares the darwin kqueue ABI (kevent64_s, same struct).
+        .mac, .ios => std.posix.system.kevent64_s,
         // usockets aliases kevent64_s → struct kevent on FreeBSD (epoll_kqueue.h),
         // so ready_polls is `struct kevent[1024]` there.
         .freebsd => std.c.Kevent,
diff --git a/src/watcher/Watcher.zig b/src/watcher/Watcher.zig
index 226a7b5..7912ea9 100644
--- a/src/watcher/Watcher.zig
+++ b/src/watcher/Watcher.zig
@@ -146,7 +146,7 @@ pub const max_eviction_count = 8096;
 // ideally, the constants above can be inlined
 const Platform = switch (Environment.os) {
     .linux => @import("./INotifyWatcher.zig"),
-    .mac, .freebsd => @import("./KEventWatcher.zig"),
+    .mac, .ios, .freebsd => @import("./KEventWatcher.zig"),
     .windows => WindowsWatcher,
     .wasm => @compileError("Unsupported platform"),
 };
diff --git a/src/workaround_missing_symbols.zig b/src/workaround_missing_symbols.zig
index 813670e..2a43c69 100644
--- a/src/workaround_missing_symbols.zig
+++ b/src/workaround_missing_symbols.zig
@@ -127,7 +127,8 @@ pub const freebsd = struct {
 pub const current = switch (bun.Environment.os) {
     .linux => linux,
     .windows => windows,
-    .mac => darwin,
+    // Skal: ios shares darwin's libsystem (same Mach-O dyld + libsystem.B).
+    .mac, .ios => darwin,
     .freebsd => freebsd,
     .wasm => struct {},
 };
