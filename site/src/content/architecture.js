// GENERATED from website/docs/architecture.html by site/scripts/extract-content.mjs
export const title = "Architecture — Skal Docs";
export const description = "";
export default "<h1>Architecture</h1>\n    <p class=\"lede\">Three runtimes — your Solid app, an embedded JS engine, and Flutter — sharing one region of memory. Here's the whole path from a signal write to a pixel.</p>\n\n    <h2>The three layers</h2>\n    <ul>\n      <li><b>Your app (SolidJS).</b> Plain Solid components compiled against Skal's <i>universal renderer</i>. Instead of creating DOM nodes, the renderer emits binary operations.</li>\n      <li><b>libskal (bun + JavaScriptCore).</b> A native library embedding a slice of bun — its JSC runtime, module loader, bytecode cache, and Skal's store engine — inside your app's process. Your bundle runs here, on a worker thread.</li>\n      <li><b>The Flutter host.</b> A thin Dart layer that drains the op ring once per frame tick and maintains a widget tree mirroring your component tree. Rendering, gestures, accessibility, and plugins are stock Flutter.</li>\n    </ul>\n\n    <h2>The shared region</h2>\n    <p>At startup, libskal allocates a single <b>6 MiB</b> block that both sides address directly for the app's whole lifetime:</p>\n    <div class=\"table-scroll\">\n      <table>\n        <tr><th>Segment</th><th>Size</th><th>Direction</th><th>Carries</th></tr>\n        <tr><td>Header</td><td>64 B</td><td>both</td><td>cursors, sequence counters, reset epochs</td></tr>\n        <tr><td>Op ring</td><td>4 MiB</td><td>JS → Dart</td><td><code>CREATE_NODE</code>, <code>INSERT_BEFORE</code>, <code>SET_PROP_*</code>, <code>SET_TEXT</code>…</td></tr>\n        <tr><td>String heap</td><td>768 KiB</td><td>JS → Dart</td><td>UTF-8 payloads referenced by string ops</td></tr>\n        <tr><td>Reply heap</td><td>256 KiB</td><td>Dart → JS</td><td>RPC results (dialogs, platform calls)</td></tr>\n        <tr><td>Event ring</td><td>rest</td><td>Dart → JS</td><td>taps, scrolls, text input, lifecycle</td></tr>\n      </table>\n    </div>\n    <p>JS sees the block as a <code>Uint8Array</code> over externally-owned bytes (JSC's no-copy ArrayBuffer); Dart sees the same bytes as a typed list over an FFI pointer. <b>There is no serialization step anywhere</b> — \"crossing the bridge\" is writing integers into a ring buffer.</p>\n\n    <h2>Life of an update</h2>\n    <ol>\n      <li>You tap a <code>&lt;Button&gt;</code>. Flutter's gesture arena resolves the tap and writes one small event (handler id + kind) into the event ring, then wakes the JS thread.</li>\n      <li>Skal's dispatcher reads the event and invokes your <code>onClick</code>. A signal updates.</li>\n      <li>Solid's reactivity re-runs only the computations that read that signal. Each affected prop becomes one op in the op ring — a 5,000-row list where one label changed emits exactly one op.</li>\n      <li>On the next frame tick, Dart drains the ring (measured at <b>0.151 ms</b> per frame, steady state), patches its node table, and marks precisely the touched widgets dirty via per-node notifiers.</li>\n      <li>Flutter rebuilds those widgets — and only those — and composites the frame.</li>\n    </ol>\n    <p>Hot props (opacity, transforms) take an even shorter path: the host tweens them on its own animation ticker, so a 60 fps animation costs <b>zero</b> bridge traffic per frame.</p>\n\n    <h2>Cold start &amp; bytecode</h2>\n    <p>Release builds ship the bundle twice: as source (<code>.cjs</code>) and as pre-compiled JavaScriptCore bytecode (<code>.jsc</code>). On launch, JSC attaches the bytecode directly — the parser never runs. That turns a several-hundred-millisecond parse into a <b>36 ms</b> eval, and a <b>56 ms</b> cold boot to first frame.</p>\n    <div class=\"callout\">The bytecode is keyed to the exact JSC version inside libskal — the toolchain builds both from the same pinned runtime, so they can never skew.</div>\n\n    <h2>Why not a virtual DOM? Why not Flutter alone? Why not RN?</h2>\n    <ul>\n      <li><b>vs. React Native:</b> RN's architecture pays for serialization and a JS-side shadow tree. Skal's renderer writes binary ops straight into memory Flutter reads — the \"bridge\" is a pointer.</li>\n      <li><b>vs. Flutter alone:</b> you keep the JS ecosystem, sub-second hot reload of app code, and a language your web team already ships in — while the rendering fidelity IS Flutter.</li>\n      <li><b>vs. WebViews:</b> nothing here is a browser. Widgets are real platform-composited Flutter widgets; the DOM never existed.</li>\n    </ul>\n\n    <div class=\"pn\">\n      <a href=\"./\"><small>Previous</small>← Getting Started</a>\n      <a class=\"next\" href=\"components.html\"><small>Next</small>Components →</a>\n    </div>";
