// GENERATED from website/docs/index.html by site/scripts/extract-content.mjs
export const title = "Getting Started — Skal Docs";
export const description = "";
export default "<h1>Getting Started</h1>\n    <p class=\"lede\">From zero to a native app running your Solid code in about three minutes (plus one coffee while the toolchain warms).</p>\n\n    <h2>Prerequisites</h2>\n    <ul>\n      <li><a href=\"https://bun.sh\">bun</a> 1.2+ — runs the CLI, the bundler, and your tests</li>\n      <li><a href=\"https://flutter.dev\">Flutter</a> 3.41+ — the rendering host (<code>flutter doctor</code> should be green for your targets)</li>\n      <li>Xcode for iOS/macOS, or an Android SDK + emulator for Android</li>\n    </ul>\n\n    <h2>Create an app</h2>\n    <pre class=\"code\"><span class=\"cm\">$</span> npm create skal my-app\n<span class=\"cm\">$</span> cd my-app</pre>\n    <p>The scaffold is a complete, runnable project:</p>\n    <pre class=\"code\">my-app/\n├── src/\n│   ├── App.jsx          <span class=\"cm\"># your Solid app — edit this</span>\n│   └── index.jsx        <span class=\"cm\"># entry: mounts &lt;App/&gt; on the bridge renderer</span>\n├── flutter-host/        <span class=\"cm\"># platform shells (android/ios/macos) + libskal</span>\n├── .maestro/smoke.yaml  <span class=\"cm\"># E2E smoke test, ready to run</span>\n└── package.json         <span class=\"cm\"># dev / build / test scripts</span></pre>\n\n    <h2>Run it</h2>\n    <pre class=\"code\"><span class=\"cm\">$</span> npm i -g @skal/cli   <span class=\"cm\"># once — or use `bun run dev:&lt;target&gt;` directly</span>\n\n<span class=\"cm\">$</span> skal dev macos       <span class=\"cm\"># fastest loop for UI work</span>\n<span class=\"cm\">$</span> skal dev android     <span class=\"cm\"># boots an emulator automatically if none is attached</span>\n<span class=\"cm\">$</span> skal dev ios         <span class=\"cm\"># iOS Simulator</span>\n<span class=\"cm\">$</span> skal dev web         <span class=\"cm\"># Solid → DOM at localhost:5173</span></pre>\n    <p><code>skal dev</code> builds the JS bundle and launches the platform host. Add <code>--hot</code> and the hot-reload server pushes every save under <code>src/</code> straight into the running app — <a href=\"tooling.html\">navigation and hot state survive</a>.</p>\n\n    <div class=\"callout\">🍻 <b>First run is slower.</b> The platform host compiles once per target (normal Flutter behavior). Every run after that is seconds.</div>\n\n    <h2>Make it yours</h2>\n    <p>Open <code>src/App.jsx</code>. It's plain SolidJS — signals, memos, effects, <code>&lt;For&gt;</code>, <code>&lt;Show&gt;</code> — with Skal's widget set instead of DOM tags:</p>\n    <pre class=\"code\"><span class=\"kw\">import</span> { createSignal } <span class=\"kw\">from</span> <span class=\"st\">'solid-js'</span>;\n<span class=\"kw\">import</span> { Column, Text, Button } <span class=\"kw\">from</span> <span class=\"st\">'skal'</span>;\n\n<span class=\"kw\">export default function</span> <span class=\"fn\">App</span>() {\n  <span class=\"kw\">const</span> [name, setName] = <span class=\"fn\">createSignal</span>(<span class=\"st\">'Skal'</span>);\n  <span class=\"kw\">return</span> (\n    &lt;<span class=\"fn\">Column</span> padding={<span class=\"nm\">24</span>} gap={<span class=\"nm\">16</span>}&gt;\n      &lt;<span class=\"fn\">Text</span> label={<span class=\"st\">`Hello, ${</span>name()<span class=\"st\">}`</span>} fontSize={<span class=\"nm\">28</span>} fontWeight={<span class=\"nm\">800</span>} /&gt;\n      &lt;<span class=\"fn\">Button</span> label=<span class=\"st\">\"Skål 🍻\"</span> onClick={() =&gt; <span class=\"fn\">setName</span>(<span class=\"st\">'World'</span>)} /&gt;\n    &lt;/<span class=\"fn\">Column</span>&gt;\n  );\n}</pre>\n    <p>When a signal changes, Solid updates exactly the widgets that read it — no virtual DOM, no reconciliation pass. The change crosses to Flutter as a handful of binary ops in shared memory. See <a href=\"components.html\">Components</a> for the full widget set.</p>\n\n    <h2>Ship it</h2>\n    <pre class=\"code\"><span class=\"cm\">$</span> skal build android   <span class=\"cm\"># release APK, JSC bytecode cache baked in</span>\n<span class=\"cm\">$</span> skal build ios       <span class=\"cm\"># release build for TestFlight</span>\n<span class=\"cm\">$</span> skal build macos\n<span class=\"cm\">$</span> skal build web       <span class=\"cm\"># static site bundle</span></pre>\n    <p>Release builds compile your bundle to JavaScriptCore bytecode, so production cold starts skip the JS parser entirely — that's the <a href=\"../#performance\">36 ms first-eval</a>.</p>\n\n    <h2>Where next</h2>\n    <ul>\n      <li><a href=\"architecture.html\">Architecture</a> — what actually happens between a signal and a pixel</li>\n      <li><a href=\"state.html\">State &amp; the Store</a> — persistent state with the native store engine</li>\n      <li><a href=\"native.html\">Wrapping pub.dev packages</a> — camera, geolocation, anything native</li>\n      <li><a href=\"tooling.html\">Hot reload &amp; dev loop</a> — sub-second reload, what state survives</li>\n      <li><a href=\"testing.html\">Testing</a> — unit tests, the headless harness, Maestro E2E</li>\n    </ul>\n\n    <div class=\"pn\">\n      <span></span>\n      <a class=\"next\" href=\"architecture.html\"><small>Next</small>Architecture →</a>\n    </div>";
