// GENERATED from website/docs/native.html by site/scripts/extract-content.mjs
export const title = "Wrapping pub.dev packages — Skal Docs";
export const description = "";
export default "<h1>Wrapping pub.dev packages</h1>\n    <p class=\"lede\">The point of rendering with Flutter isn't just pixels — it's <a href=\"https://pub.dev\">pub.dev</a>: tens of thousands of maintained native plugins. Skal's codegen turns any Flutter widget into a JSX component and any plugin API into an awaitable JS call. No platform channels, no per-plugin JS glue.</p>\n\n    <h2>Widgets — one line of config</h2>\n    <p>List the package in your app's <code>skal_codegen.yaml</code>:</p>\n    <pre class=\"code\"><span class=\"cm\"># flutter-host/lib/skal_codegen.yaml</span>\npackages:\n  - qr_flutter\n  - flutter_map</pre>\n    <p>Then run the generator:</p>\n    <pre class=\"code\"><span class=\"cm\">$</span> bun run codegen</pre>\n    <p>The builder introspects each package's public widgets with Dart's analyzer — constructors, named constructors, parameter types — and emits adapters plus a manifest. On the JS side, typed components appear under <code>skal-flutter</code>:</p>\n    <pre class=\"code\"><span class=\"kw\">import</span> { QrImageView, FlutterMap, TileLayer } <span class=\"kw\">from</span> <span class=\"st\">'skal-flutter'</span>;\n\n&lt;<span class=\"fn\">QrImageView</span> data=<span class=\"st\">\"https://skal.dev\"</span> size={<span class=\"nm\">220</span>} /&gt;</pre>\n    <div class=\"callout\">Named constructors surface as their own components — shimmer's <code>Shimmer.fromColors</code> becomes <code>&lt;ShimmerFromColors&gt;</code>. Prop types round-trip: enums, colors, durations, edge insets, gradients, text styles, value classes, even <code>List&lt;Marker&gt;</code>-style value children as JSX children.</div>\n\n    <h2>Headless capabilities — services</h2>\n    <p>Most native capabilities have no widget: geolocation, biometrics, share, clipboard, permissions. Declare a <b>service</b> and the class's static methods become awaitable RPCs; a <code>$</code>-suffixed call subscribes a Dart <code>Stream</code> and returns an unsubscribe function:</p>\n    <pre class=\"code\"><span class=\"cm\"># skal_codegen.yaml</span>\nservices:\n  geo:\n    package: geolocator\n    class: Geolocator</pre>\n    <pre class=\"code\"><span class=\"kw\">import</span> { createSkalService } <span class=\"kw\">from</span> <span class=\"st\">'skal/runtime'</span>;\n\n<span class=\"kw\">const</span> geo = <span class=\"fn\">createSkalService</span>(<span class=\"st\">'geo'</span>);\n<span class=\"kw\">const</span> pos = <span class=\"kw\">await</span> geo.<span class=\"fn\">getCurrentPosition</span>();\n<span class=\"kw\">const</span> stop = geo.<span class=\"fn\">getPositionStream$</span>((p) =&gt; <span class=\"fn\">setPos</span>(p));</pre>\n    <p>geolocator wraps with <b>zero hand-written Dart</b> — its API is already static. A plugin with an instance-shaped API (local_auth) needs one tiny static forwarder class, and that forwarder is walked by codegen like any package:</p>\n    <pre class=\"code\"><span class=\"cm\">// flutter-host/lib/adapters/auth_service.dart — the ENTIRE integration</span>\n<span class=\"kw\">class</span> <span class=\"fn\">AuthService</span> {\n  <span class=\"kw\">static final</span> _auth = <span class=\"fn\">LocalAuthentication</span>();\n  <span class=\"kw\">static</span> Future&lt;bool&gt; <span class=\"fn\">authenticate</span>(String reason) =&gt;\n      _auth.<span class=\"fn\">authenticate</span>(localizedReason: reason);\n}</pre>\n\n    <h2>Stateful widgets — hosts</h2>\n    <p>Widgets whose constructor needs a live controller (camera preview, webview, video) use the <b>host pattern</b>: you write a ~15-line factory function; its parameters become JSX props, and the controller's public methods become imperative calls on a ref — one RPC op out, the reply back through the reply heap:</p>\n    <pre class=\"code\"><span class=\"kw\">import</span> { createSkalRef } <span class=\"kw\">from</span> <span class=\"st\">'skal/runtime'</span>;\n\n<span class=\"kw\">const</span> cam = <span class=\"fn\">createSkalRef</span>();\n&lt;<span class=\"fn\">Camera</span> ref={cam} cameraName=<span class=\"st\">\"back\"</span> /&gt;\n<span class=\"kw\">await</span> cam.<span class=\"fn\">takePicture</span>();\ncam.<span class=\"fn\">frames$</span>((f) =&gt; ...);   <span class=\"cm\">// Dart Streams subscribe with $</span></pre>\n\n    <h2>The skip report</h2>\n    <p>After every run, anything codegen could not map is listed in <code>lib/skal_codegen.json</code> under <code>\"skipped\"</code> — with the reason and the exact remedy. Never diff generated output to find out what's missing; read the report. Many skips are rescued with zero Dart via <code>overrides:</code> in the same yaml — pin a generic widget's type arguments, supply a <code>const:</code> expression for one unmappable parameter, mark an indexed builder with <code>builder: true</code>, or hold a controller as an opaque JS-side handle with <code>handle: true</code>.</p>\n\n    <h2>The escape-hatch ladder</h2>\n    <p>When codegen can't map a library, escalate one rung at a time — every rung is a shipped pattern, and the line counts below are from real plugins:</p>\n    <ol>\n      <li><b><code>overrides:</code> in the yaml</b> — one bad param, generic type args. <em>0 lines of Dart.</em></li>\n      <li><b>Static forwarder + <code>services:</code></b> — instance-shaped plugin APIs. <em>~10–25 lines</em> (local_auth: 24).</li>\n      <li><b>Platform-quirk shim</b> — a platform needs an argument codegen can't encode (share_plus wants a <code>sharePositionOrigin</code> rect on iPad/iOS 26). <em>~8 lines.</em></li>\n      <li><b><code>hosts:</code> factory</b> — live controller, async init. <em>~15–20 lines.</em></li>\n      <li><b>A plain local widget class</b> — write normal Flutter in <code>lib/adapters/</code>; codegen walks <em>your</em> code like any pub package. Same manifest, same <code>'skal-flutter'</code> import.</li>\n      <li><b>Raw registry</b> — <code>SkalRegistry.registerWidget(name, (n, bridge) =&gt; …)</code> for full manual control. Last resort; props read untyped off the node.</li>\n    </ol>\n    <div class=\"callout\">Hand-written Dart is codegen <b>input</b>, not a parallel system — a forwarder or local widget gets the same typed prop readers, stream plumbing, and JSX import synthesis as any pub package. Zero JS-side glue at every rung except the last.</div>\n\n    <h2>Permissions</h2>\n    <p>Declare intent once in <code>flutter-host/skal-permissions.json</code>:</p>\n    <pre class=\"code\">{ <span class=\"st\">\"camera\"</span>: <span class=\"st\">\"Scan QR codes\"</span>, <span class=\"st\">\"location\"</span>: <span class=\"st\">\"Show places near you\"</span> }</pre>\n    <p><code>bun run link</code> translates it into every platform dialect — iOS + macOS <code>Info.plist</code> usage strings, macOS App Sandbox entitlements, <code>AndroidManifest</code> entries, and permission_handler's <code>PERMISSION_*</code> Podfile macros (without which every request answers <code>permanentlyDenied</code>). Idempotent, add-only; your hand-edited wording is never reverted.</p>\n\n    <h2>When codegen isn't enough</h2>\n    <ul>\n      <li><b>Non-void callbacks</b> (<code>bool Function(T)</code>) need a synchronous JS→Dart return, which would block the frame — permanently out of scope. Restructure around a prop or a ref method.</li>\n      <li><b>Builders keyed by a domain object</b> (not an index) have nothing to key a subtree by — indexed builders work via <code>builder: true</code>.</li>\n      <li><b>Payloads are for data, not blobs</b> — strings beyond the heap sizes truncate. Pass paths and handles, not file contents.</li>\n    </ul>\n\n    <div class=\"pn\">\n      <a href=\"state.html\"><small>Previous</small>← State &amp; the Store</a>\n      <a class=\"next\" href=\"tooling.html\"><small>Next</small>Hot reload &amp; dev loop →</a>\n    </div>";
