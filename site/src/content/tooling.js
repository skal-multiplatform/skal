// GENERATED from website/docs/tooling.html by site/scripts/extract-content.mjs
export const title = "Hot reload & dev loop — Skal Docs";
export const description = "";
export default "<h1>Hot reload &amp; the dev loop</h1>\n    <p class=\"lede\">Save-to-screen in under a second — without losing your place in the app. How live reload works, what survives it, and the rest of the daily loop.</p>\n\n    <h2>Hot reload</h2>\n    <p><code>skal dev &lt;target&gt; --hot</code> watches <code>src/</code>. On save, the new bundle is pushed to the running app and re-evaluated <b>inside the live JS VM</b> — no process restart, no native rebuild:</p>\n    <pre class=\"code\"><span class=\"st\">↻</span> App.jsx changed — reloaded in <span class=\"nm\">380 ms</span>\n  <span class=\"cm\">navigation + hot state preserved</span></pre>\n    <p>The outgoing generation tears down cleanly (its Solid roots dispose, the host resets the widget tree by id), then the new bundle mounts in place. What carries across:</p>\n    <ul>\n      <li><b>Navigation</b> — <code>createRouter</code> stacks restore, so you stay on the screen you were styling.</li>\n      <li><b>Marked state</b> — <code>createHotState(initial, key?)</code> is a drop-in <code>createSignal</code> that survives reloads (selected tab, form draft, scroll target).</li>\n      <li><b>The store</b> — <code>createSkalStore</code> data was never in the bundle to begin with.</li>\n    </ul>\n    <div class=\"callout\">A bundle that throws on reload doesn't brick the session — Skal re-mounts the last good bundle and prints the error, so you fix the typo and save again.</div>\n    <p>Prefer manual control? Run the bundle watcher (<code>bun run dev</code>) in a second terminal and reload with <code>r</code> in the <code>flutter run</code> one. In release builds, all reload machinery is compiled out — zero overhead ships.</p>\n\n    <h2>The rest of the loop</h2>\n    <div class=\"table-scroll\">\n      <table>\n        <tr><th>Command</th><th>What it does</th></tr>\n        <tr><td><code>skal dev &lt;target&gt;</code></td><td>build + launch — add <code>--hot</code> for reload-on-save; auto-boots an emulator if none is attached</td></tr>\n        <tr><td><code>bun run test</code></td><td>host widget tests (<code>flutter test</code>) — see <a href=\"testing.html\">Testing</a></td></tr>\n        <tr><td><code>bun run test:e2e</code></td><td>Maestro flows against the real app — see <a href=\"testing.html\">Testing</a></td></tr>\n        <tr><td><code>bun run codegen</code></td><td>regenerate pub.dev widget adapters — see <a href=\"native.html\">Wrapping packages</a></td></tr>\n        <tr><td><code>skal build &lt;target&gt;</code></td><td>shippable release artifact with the bytecode cache baked in</td></tr>\n      </table>\n    </div>\n\n    <h2>Benchmarks</h2>\n    <p>The <a href=\"../#performance\">published numbers</a> come from the kitchen-sink demo: boot timing prints on every launch (<code>[skal] init=… boot=…</code>), the JS tab's bench buttons drive prop-write throughput and the 10,000-item feed, and the in-app meter shows per-frame drain cost live. Methodology and budget invariants: <a href=\"https://github.com/skal-multiplatform/skal/blob/main/docs/PERFORMANCE.md\">docs/PERFORMANCE.md</a>. Run it on your own hardware:</p>\n    <pre class=\"code\"><span class=\"cm\">$</span> git clone https://github.com/skal-multiplatform/skal &amp;&amp; cd skal\n<span class=\"cm\">$</span> SKAL_PREBUILT=1 bun run setup   <span class=\"cm\"># prebuilt libskal — no toolchain needed</span>\n<span class=\"cm\">$</span> bun run dev:android             <span class=\"cm\"># kitchen-sink; watch the boot line + perf meter</span></pre>\n    <p>Every number on this site is reproducible that way. If your hardware disagrees with us, open an issue — that's a bug in the claim or a bug in the code, and either way we want it.</p>\n\n    <div class=\"pn\">\n      <a href=\"native.html\"><small>Previous</small>← Wrapping pub.dev packages</a>\n      <a class=\"next\" href=\"testing.html\"><small>Next</small>Testing →</a>\n    </div>";
